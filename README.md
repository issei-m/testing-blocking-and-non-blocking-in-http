# Testing blocking/non-blocking IO in HTTP

```
docker compose up --build
```

```
curl http://localhost:8888/php/
curl http://localhost:8888/ruby/
curl http://localhost:8888/python/
curl http://localhost:8888/go/
```
